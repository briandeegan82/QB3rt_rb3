# Template per-unit profile. Copy to <id>.conf and fill in per robot, then:
#   stamp/stamp_unit.sh --unit <id>
SSH_HOST=ubuntu@192.168.0.XX
HOSTNAME=qb3rt-XX
DOMAIN_ID=XX
STATIC_IP=192.168.0.XX   # pin IP to match the unit id (omit to keep DHCP)
USB_RPLIDAR_SERIAL=
USB_WAVE_ROVER_SERIAL=
WIFI_KEEP=ros_net_5G,ROS_NET
WIFI_SSID=
WIFI_PSK=
